You are building a TypeScript CLI project for Termux/Android.

Create the initial scaffolding for "android-llm-cli" (binary name: a-llmcli):

1. **package.json**: name "android-llm-cli", type "module", bin: { "a-llmcli": "./dist/index.js" }, engines.node >=20, scripts: dev, build, test. Dependencies: ink, react, meow. DevDeps: typescript, @types/react, vitest, @ink-testing-library.

2. **tsconfig.json**: strict mode, module "ES2022", moduleResolution "Bundler", noUncheckedIndexedAccess true, jsx react-jsx, target ES2022.

3. **tsconfig.build.json**: extends tsconfig.json, outDir "dist", exclude tests.

4. **vitest.config.ts**: basic vitest config for TypeScript.

5. **src/index.ts**: shebang #!/usr/bin/env node, meow CLI stub that parses --help, --version, --config, --debug flags. Import ink and render a basic "<App>" component. The App component should export a simple Text that says "android-llm-cli v0.1.0".

Commit all changes with message "feat(T-01): scaffolding and toolchain".
